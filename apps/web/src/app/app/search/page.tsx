import { ComingSoon } from "@/components/coming-soon";

export default function SearchPage() {
  return (
    <ComingSoon
      title="Search"
      description="Search across projects, tracks, motifs, and exported assets."
    />
  );
}

