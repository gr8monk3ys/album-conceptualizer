# NOTE: Vercel does NOT read this file for this project. Its Python builder
# detects pyproject.toml and installs from that ("Installing required
# dependencies from pyproject.toml..." in the build log), so the base
# `dependencies` list is what actually ships and optional extras never do.
#
# Kept for local/non-Vercel use (`pip install -r requirements.txt`), and as
# the record of that behaviour -- adding a line here and expecting the deploy
# to change is a trap that cost several rounds to spot.
#
# Consequences on Vercel today:
#   * [music] is absent -> MIDI/ChordPro/MusicXML export answer 501 with an
#     explanatory message. To fix, those deps must move into pyproject's base
#     `dependencies`, which is a real decision, not a deploy tweak.
#   * [ai] is absent -> agent endpoints answer 503, which is intended.
#
# The fastapi upper bound is REQUIRED -- see pyproject.toml.
-e .
