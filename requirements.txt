# Base engine dependencies only -- generated from pyproject.toml.
# The [ai] extra (crewai, langchain, chromadb) is EXCLUDED: far past
# Vercel's bundle limit, and the agent endpoints already 503 without it.
# The fastapi upper bound is REQUIRED -- see pyproject.toml.
pydantic>=2.11.7
pydantic-settings>=2.8.1
python-dotenv>=1.1.1,<1.3.0
pyyaml>=6.0.3
httpx>=0.28.1
tenacity>=9.1.3
rich>=14.3.2
fastapi>=0.128.2,<0.137
uvicorn[standard]>=0.37.0
stripe>=12.0.0
